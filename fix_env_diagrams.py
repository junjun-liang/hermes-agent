#!/usr/bin/env python3
"""替换执行环境隔离文档中的三个 ASCII 流程图为 Mermaid"""

import glob

files = glob.glob('/home/meizu/Documents/my_agent_project/hermes-agent/*执行环境*.md')
filepath = files[0]

with open(filepath, 'r', encoding='utf-8') as f:
    content = f.read()

# 1. 执行环境初始化流程 (行 438)
old_init = '''### 3.1 执行环境初始化流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       执行环境初始化流程                                      │
│                                                                             │
│  ┌─────────────────────────────┐                                            │
│  │ 选择 env_type                │                                            │
│  │ local/docker/ssh/modal/     │                                            │
│  │ daytona/singularity          │                                            │
│  └─────────────┬───────────────┘                                            │
│                │                                                            │
│                ▼                                                            │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ get_or_create_env(env_type, **kwargs)                │                  │
│  └───────────────────────┬──────────────────────────────┘                  │
│                          │                                                  │
│                ┌─────────┴─────────┐                                       │
│                ▼                   ▼                                       │
│            已有缓存              无缓存                                    │
│                │                   │                                       │
│                ▼                   ▼                                       │
│  ┌───────────────────┐  ┌──────────────────────────────────┐              │
│  │ 使用缓存环境       │  └───────────────┬──────────────────┘              │
│  └────────┬──────────┘                  │                                  │
│           │                  ┌──────────┴──────────┐                       │
│           │                  ▼                     ▼                       │
│           │         ┌───────────────────────────────────────────┐         │
│           │         │ 根据 env_type 创建对应环境                  │         │
│           │         └───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬─┘         │
│           │             ▼   ▼   ▼   ▼   ▼   ▼   ▼   ▼   ▼   ▼           │
│           │      ┌────┐┌────┐┌────┐┌────┐┌────┐┌────┐                  │
│           │      │Local││Dkr ││SSH ││Modl││Dayt││Sing│                  │
│           │      └─┬──┘└─┬──┘└─┬──┘└─┬──┘└─┬──┘└─┬──┘                  │
│           │        │     │     │     │     │     │                       │
│           │        └─────┴─────┴─────┴─────┴─────┘                       │
│           │                      │                                       │
│           │                      ▼                                       │
│           │         ┌────────────────────────────────┐                   │
│           │         │ init_session()                 │                   │
│           │         │                                │                   │
│           │         │ 1. bash -l -c "export -p"     │                   │
│           │         │ 2. 解析 export VAR=value       │                   │
│           │         │ 3. 保存到 snapshot.json        │                   │
│           │         │                                │                   │
│           │         │ 作用：捕获登录 shell 环境变量    │                   │
│           │         │      后续执行时 source 复用     │                   │
│           │         └────────────────┬───────────────┘                   │
│           │                          │                                   │
│           │                          ▼                                   │
│           │         ┌────────────────────────────────┐                   │
│           │         │ FileSyncManager.sync()         │                   │
│           │         │                                │                   │
│           │         │ 1. 扫描本地：                   │                   │
│           │         │    ~/.hermes/skills/           │                   │
│           │         │    ~/.hermes/credentials/      │                   │
│           │         │                                │                   │
│           │         │ 2. 对比远程文件列表             │                   │
│           │         │                                │                   │
│           │         │ 3. 批量上传新文件：              │                   │
│           │         │    SSH: tar | ssh | tar        │                   │
│           │         │    Modal: tar+base64 | stdin   │                   │
│           │         │    Daytona: SDK multipart      │                   │
│           │         │                                │                   │
│           │         │ 4. 批量删除远程文件             │                   │
│           │         │                                │                   │
│           │         │ 5. 更新远程文件缓存             │                   │
│           │         └────────────────┬───────────────┘                   │
│           │                          │                                   │
│           └──────────┬───────────────┘                                   │
│                      │                                                   │
│                      ▼                                                   │
│           ┌──────────────────────┐                                       │
│           │  环境就绪 ✓           │                                       │
│           └──────────────────────┘                                       │
└───────────────────────────────────────────────────────────────────────────┘
```'''

new_init = '''### 3.1 执行环境初始化流程

```mermaid
flowchart TD
    A[选择 env_type\nlocal/docker/ssh/modal/daytona/singularity] --> B[get_or_create_env]
    B --> C{环境缓存？}
    
    C -->|已有缓存 | D[使用缓存环境]
    C -->|无缓存 | E[创建新环境]
    
    E --> F{env_type 类型}
    F -->|local| G1[LocalEnv]
    F -->|docker| G2[DockerEnv]
    F -->|ssh| G3[SSHEnv]
    F -->|modal| G4[ModalEnv]
    F -->|daytona| G5[DaytonaEnv]
    F -->|singularity| G6[SingularityEnv]
    
    G1 --> H[init_session]
    G2 --> H
    G3 --> H
    G4 --> H
    G5 --> H
    G6 --> H
    
    H --> I["1. bash -l -c 'export -p'\n2. 解析 export VAR=value\n3. 保存到 snapshot.json"]
    
    I --> J[FileSyncManager.sync]
    
    J --> K["1. 扫描本地文件\n   ~/.hermes/skills/\n   ~/.hermes/credentials/\n2. 对比远程文件列表\n3. 批量上传新文件\n4. 批量删除远程文件\n5. 更新远程文件缓存"]
    
    D --> L[环境就绪]
    K --> L
```'''

# 2. 命令执行流程 (行 518)
old_exec = '''### 3.2 命令执行流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          命令执行流程                                         │
│                                                                             │
│  ┌─────────────────────────────┐                                            │
│  │ execute_command(command,    │                                            │
│  │                 cwd, env,   │                                            │
│  │                 timeout)    │                                            │
│  └─────────────┬───────────────┘                                            │
│                │                                                            │
│                ▼                                                            │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ get_or_create_env(env_type, **kwargs)                │                  │
│  │ 获取或创建执行环境                                    │                  │
│  └───────────────────────┬──────────────────────────────┘                  │
│                          │                                                  │
│                          ▼                                                  │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ source_session_snapshot()                            │                  │
│  │                                                      │                  │
│  │ 从 snapshot.json 加载环境变量                         │                  │
│  │ 执行：source /path/to/snapshot.json                  │                  │
│  │                                                      │                  │
│  │ 作用：恢复上次会话的环境变量                          │                  │
│  └───────────────────────┬──────────────────────────────┘                  │
│                          │                                                  │
│                          ▼                                                  │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ 应用 cwd 和 env 覆盖                                   │                  │
│  └───────────────────────┬──────────────────────────────┘                  │
│                          │                                                  │
│                          ▼                                                  │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ 执行命令 (spawn-per-call)                            │                  │
│  │                                                      │                  │
│  │ local:   subprocess.Popen                            │                  │
│  │ docker:  docker exec / start + attach                │                  │
│  │ ssh:     exec_command                                │                  │
│  │ modal:   container.run                               │                  │
│  │ daytona: SDK.execute                                │                  │
│  │ singularity: singularity exec                       │                  │
│  └───────────────────────┬──────────────────────────────┘                  │
│                          │                                                  │
│                          ▼                                                  │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ 等待完成 + 收集输出                                   │                  │
│  │                                                      │                  │
│  │ stdout: 实时读取/批量读取                            │                  │
│  │ stderr: 实时读取/批量读取                            │                  │
│  │ returncode: 等待退出码                               │                  │
│  │ timeout:   超时控制                                  │                  │
│  └───────────────────────┬──────────────────────────────┘                  │
│                          │                                                  │
│                          ▼                                                  │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ 更新会话快照                                         │                  │
│  │                                                      │                  │
│  │ bash -l -c "export -p"                              │                  │
│  │ 捕获执行后新导出的变量                                │                  │
│  │ 更新到 snapshot.json                                 │                  │
│  └───────────────────────┬──────────────────────────────┘                  │
│                          │                                                  │
│                          ▼                                                  │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ 返回结果                                             │                  │
│  │                                                      │                  │
│  │ {stdout, stderr, returncode, timeout}                │                  │
│  └──────────────────────────────────────────────────────┘                  │
└─────────────────────────────────────────────────────────────────────────────┘
```'''

new_exec = '''### 3.2 命令执行流程

```mermaid
flowchart TD
    A[execute_command\ncommand, cwd, env, timeout] --> B[get_or_create_env]
    B --> C[source_session_snapshot\n加载 snapshot.json]
    C --> D[应用 cwd 和 env 覆盖]
    D --> E[执行命令 spawn-per-call]
    
    E --> F{env_type 类型}
    F -->|local| G1[subprocess.Popen]
    F -->|docker| G2[docker exec/attach]
    F -->|ssh| G3[exec_command]
    F -->|modal| G4[container.run]
    F -->|daytona| G5[SDK.execute]
    F -->|singularity| G6[singularity exec]
    
    G1 --> H[等待完成 + 收集输出]
    G2 --> H
    G3 --> H
    G4 --> H
    G5 --> H
    G6 --> H
    
    H --> I["stdout/stderr\nreturncode\ntimeout"]
    I --> J[更新会话快照\nbash -l -c 'export -p']
    J --> K[返回结果]
```'''

# 3. 环境变量隔离流程 (行 614)
old_env = '''### 3.3 环境变量隔离流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        环境变量隔离流程                                        │
│                                                                             │
│  ┌─────────────────────────────┐                                            │
│  │ 进程启动                     │                                            │
│  └─────────────┬───────────────┘                                            │
│                │                                                            │
│                ▼                                                            │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ 1. 继承最小系统环境变量                               │                  │
│  │                                                      │                  │
│  │ PATH=/usr/bin:/bin                                  │                  │
│  │ HOME=/home/user                                     │                  │
│  │ LANG=C.UTF-8                                        │                  │
│  │ HERMES_HOME=~/.hermes                               │                  │
│  └───────────────────────┬──────────────────────────────┘                  │
│                          │                                                  │
│                          ▼                                                  │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ 2. source 会话快照 (snapshot.json)                   │                  │
│  │                                                      │                  │
│  │ source /path/to/snapshot.json                       │                  │
│  │                                                      │                  │
│  │ 恢复上次会话导出的所有环境变量                        │                  │
│  └───────────────────────┬──────────────────────────────┘                  │
│                          │                                                  │
│                          ▼                                                  │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ 3. 应用命令特定的 env 覆盖                             │                  │
│  │                                                      │                  │
│  │ os.environ.update(custom_env)                       │                  │
│  │                                                      │                  │
│  │ 例如：PYTHONPATH, CUSTOM_VAR 等                       │                  │
│  └───────────────────────┬──────────────────────────────┘                  │
│                          │                                                  │
│                          ▼                                                  │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ 4. 执行命令                                          │                  │
│  │                                                      │                  │
│  │ subprocess.Popen(cmd, env=final_env)                │                  │
│  └───────────────────────┬──────────────────────────────┘                  │
│                          │                                                  │
│                          ▼                                                  │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ 5. 命令完成，进程结束                                 │                  │
│  │                                                      │                  │
│  │ 环境变量随进程销毁                                    │                  │
│  │ 不污染父进程或其他进程                                │                  │
│  └───────────────────────┬──────────────────────────────┘                  │
│                          │                                                  │
│                          ▼                                                  │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │ 6. 更新会话快照                                     │                  │
│  │                                                      │                  │
│  │ bash -l -c "export -p"                              │                  │
│  │ 捕获新导出的变量                                     │                  │
│  │ 更新到 snapshot.json                                 │                  │
│  └──────────────────────────────────────────────────────┘                  │
└─────────────────────────────────────────────────────────────────────────────┘
```'''

new_env = '''### 3.3 环境变量隔离流程

```mermaid
flowchart TD
    A[进程启动] --> B["1. 继承最小系统环境变量\nPATH, HOME, LANG\nHERMES_HOME"]
    B --> C["2. source 会话快照\nsnapshot.json"]
    C --> D["3. 应用命令特定的 env 覆盖\nos.environ.update"]
    D --> E["4. 执行命令\nsubprocess.Popen\nenv=final_env"]
    E --> F["5. 命令完成，进程结束\n环境变量随进程销毁\n不污染父进程"]
    F --> G["6. 更新会话快照\nbash -l -c 'export -p'\n更新 snapshot.json"]
```'''

# 执行替换
content = content.replace(old_init, new_init)
content = content.replace(old_exec, new_exec)
content = content.replace(old_env, new_env)

with open(filepath, 'w', encoding='utf-8') as f:
    f.write(content)

print("✅ 成功替换 3 个 ASCII 流程图为 Mermaid")
print(f"📄 文件：{filepath}")
print("\n替换详情:")
print("1. 执行环境初始化流程 (行 438)")
print("2. 命令执行流程 (行 518)")
print("3. 环境变量隔离流程 (行 614)")
